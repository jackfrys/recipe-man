{
  "name": "recipe-man",
  "version": "1.0.0",
  "description": "CS3200 Database Design project",
  "main": "server.js",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "mongoose": "^4.13.17",
    "q": "^1.5.1",
    "request": "^2.88.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "private": true,
  "author": "Jack Frysinger"
}
